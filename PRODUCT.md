# pinkdooHub Product Context

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 顾客：从商城浏览拼豆体验与材料套装，维护购物车、提交和管理自己的订单，创建或管理自己的预约，并在会员中心查看资料与钱包。
- 店铺 ADMIN+：登录或恢复会话后直接进入独立“店铺工作台”，从预约审核、预约跟进、订单、商品、商城库存流水、门店用豆库存、营业日历、桌台和用户九个入口开展管理工作；不进入顾客商城、会员中心或普通会员钱包流程。

## Product Purpose

pinkdooHub 是拼豆店的统一业务工作台。它把顾客选购、订单履约、商品配置、权威库存与审计记录放进同一条可追溯流程，减少前台浏览与后台管理之间的信息断层。

## Positioning

产品首先服务小型拼豆门店的真实运营，而不是通用电商模板。顾客侧强调清楚、轻松地找到合适的体验或材料套装；管理侧强调准确、可复核地完成高频操作。界面应该友好，但不能用装饰掩盖价格、库存、状态和操作后果。

## Operating Context

- 正式前端基于 Taro、React 与 TypeScript；当前 Release 只交付微信小程序 Gate A 内部测试版。
- H5、支付宝和抖音构建能力用于兼容性验证，不代表当前发布承诺。
- 核心场景为移动端触控；顾客与 ADMIN+ 使用同一应用中的主包和管理分包。
- FastAPI 服务端是商品、订单、库存、权限与审计的权威来源。

## Capabilities and Constraints

- 保留现有认证、商品、购物车、订单、管理商品、库存、用户与审计功能及其状态语义。
- 顾客一级信息架构固定为“商城 / 预约 / 购物车 / 会员中心”四个根页面；商品浏览只属于商城，商品详情、订单列表、下单确认、订单/预约详情、认证、钱包明细和全部管理工作流仍是没有常驻底栏的二级页面。
- 会员中心按身份与订单、可用余额、完整展开的个人资料、退出账号排列；订单保留全部和待支付／已支付／已完成入口，钱包金额与充值能力使用真实服务端数据。
- 无底栏启动分流页只在服务端确认身份后决定落点：Guest/普通 USER 进入商城，ADMIN+ 进入独立店铺工作台。管理员误入任一顾客根页时立即回到工作台，且在角色分支前不挂载商城、顾客预约、顾客订单或普通客户钱包请求。
- 工作台按“待处理 / 商品与库存 / 门店与权限”分为三组，提供九个实际工作入口。预约入口通过专用汇总 API 展示可审核和取消变更数量；顾客商城、预约页及预约 Tab 展示同一未读结果数。订单、桌台与资金结果已按 P1.2/P1.3 接入，不以列表首屏条数拼接数量。管理员仍使用独立工作台，无顾客购买入口或专属底栏。
- 不在视觉改版中发明后端尚不存在的收藏、优惠、支付、启用用户或商品删除恢复能力。
- 价格、库存、权限、订单状态和幂等结果均以服务端响应为准；客户端不得用展示状态替代业务判断。
- 写操作的超时、结果未知和安全重试边界必须保持现有实现。
- 不引入未经确认的新第三方运行时依赖，也不改变当前发布平台范围。

## Brand Commitments

- 品牌名保持 `pinkdooHub`，视觉方向采用已选定的 A 方案 “Ribbon Ledger”。
- 使用克制的莓果粉与暖白色；渐变只在相邻色阶间制造轻微纹理，不横跨大色域。
- 透明与模糊用于表达顶部导航、筛选和操作层级，不把所有容器都做成玻璃卡片。
- 四个顾客根页面使用“瓷白丝带托盘”底部导航：等宽入口、单一莓果选中态和克制抬升动效延续 Ribbon Ledger，不照搬参考产品的品牌资产或中央主按钮。
- 中文信息排版优先保证短行、明确层级、数字可扫读和移动端可读性。
- 当前版本不使用 Hello Kitty 或其他角色图案；真实商品图片继续来自现有业务数据。

## Evidence on Hand

- 仓库中的需求、API、数据库、前端架构、测试与现有实现。
- 本地 A 方案参考稿：`.ui-design-explorations/pinkdoo-ui-refresh/screenshots/ribbon-ledger.jpg`，仅用于本地评审，不进入版本库。
- 现有 Product API 返回的真实商品图片、名称、类型、价格、状态和库存数据。
- 当前没有已确认授权的角色形象资产，因此不把角色图案作为品牌系统的一部分。

## Product Principles

1. 先让关键信息一眼可读，再增加视觉个性。
2. 顾客侧减少选择负担，管理侧减少误操作与重复确认成本。
3. 同一种状态、操作和数字在跨页面场景中保持一致表达。
4. 装饰必须支持层级、触控或内容辨识，不能与真实商品争夺注意力。
5. 视觉升级不得改变已有功能、接口契约或权威业务边界。

## Accessibility & Inclusion

- 交互控件保持足够的触控面积，并提供明确的禁用、加载、错误和成功状态。
- 底部导航每项保持至少 44px 触控区域，选中态同时依靠图标底座、实心图标、字重和轻微位移表达，不只依赖颜色。
- 正文、状态和按钮文字保持可读对比度；状态不能只依赖颜色区分。
- 尊重系统减少动态效果偏好，不使用影响任务完成的动画。
- 中文正文控制行宽与行高；连续 ID、订单号等长内容允许安全换行，但普通文案不使用逐字断行。

## 预约筛选偏好（2026-09-14）

预约状态名称明确区分门店“已拒绝”和“已过期未处理”。预约筛选选项必须完整展示，使用等宽换行布局，不使用横向滚动轨道来容纳状态选项；保留原胶囊样式与触控尺寸。

### 订单与桌台交接 P1.2（2026-09-14）

首页待付款占台归桌台入口，普通订单待付款归订单入口，避免同一付款出现两个红圈。订单收款后进入履约；顾客看到含金额和计时结果的一件消息。会员中心与 Tab 镜像订单/桌台事项，预约页保持预约范围。提醒页复用现有三项直接展示筛选，旧会话结果始终可打开当次记录。微信及资金结果 P1.3 不在本阶段启用。


### 资金结果 P1.3（2026-09-14）

门店代客扣款与退款合并到对应订单结果；独立余额调整按流水逐笔展示“余额变动”，顾客可核对金额、说明和当时余额。首页、会员中心及会员 Tab 镜像同一未读计数，完整详情展示后才清除该事件，已读结果仍可从资金明细入口复看。线下退款明确标为门店登记，微信仍延期。下一步为 P1.4 联合验收。

### P2.1/P2.2 预约跟进（2026-09-15）

工作台从七入口扩展为八入口：待处理组新增“预约跟进”，独立显示店休待联系/过期待跟进，不混入预约审核计数。店休联系与过期跟进在管理员分包以双列筛选完整显示；详情就近提供联系方式、处理结果、必填说明和历史。未接通、联系方式不可用仍需跟进；实际联系或明确结束后才移出队列。顾客已读、拨号与门店完成分别管理。

- 门店用豆库存按未拆封整包独立记录，221 色双列文本台账、双维筛选和整批二次确认；不联动商城 10g 库存，也不产生库存提醒。具体业务规则见 `docs/01_requirements/store_bead_stock_plan.md`。
