# 预约拒绝文案与筛选换行检查

日期：2026-09-14。本次为既有 Product Design 修订的局部跟进，沿用 Ribbon Ledger 配色、胶囊和面板。

## 变更与语义

- 原“未能确认”对应门店执行拒绝，统一改为“已拒绝”；详情使用“拒绝原因”，顾客结果与提醒明确门店拒绝。过期未处理仍独立展示，不计为已拒绝。
- 管理预约六个筛选使用三列两行等宽布局；顾客预约五个筛选使用三列换行。取消横向滚动，保留原筛选行为。
- 同步服务端展示常量、前端严格响应校验、需求/API/集成文档；状态值与数据结构不变，无新增迁移或依赖。

## 页面验证

使用临时 H5 服务、虚构管理员和空预约列表，在 Codex 应用内浏览器检查真实构建页面，无真实业务数据。

| 场景 | 结果 | 截图 |
| --- | --- | --- |
| 390px 管理预约 | 六个状态完整显示，三列两行；页面 scrollWidth = innerWidth = 390 | [390px](reservation-filters-390.png) |
| 320px 管理预约 | 点击“已拒绝”后选中态正确；三列两行，页面 scrollWidth = innerWidth = 320 | [320px](reservation-filters-320.png) |

顾客端同步修改样式和文案并通过前端测试；本轮管理员预览账号访问顾客页被角色路由重定向，未将该尝试计为顾客页视觉验收。微信真机显示未验证。

## 自动检查

- 预约后端：110 passed，19 skipped（需要 MySQL 的测试未在本轮运行）。
- 前端全量：106 suites，816 tests passed。
- TypeScript、ESLint、Stylelint、差异空白检查通过。
- H5 与微信 CI 构建通过；微信产物检查通过，release_eligible=false。

前后端展示协议需同步加载；本轮未发布或操作持久数据库。临时预览与浏览器页面在检查后关闭。
