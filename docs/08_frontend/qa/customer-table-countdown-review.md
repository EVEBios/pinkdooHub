# 顾客本次桌台倒计时检查（2026-09-14）

## 改动与边界

- `pages/table-history` 增加独立倒计时组件及详情读取 Hook；付款、各时长组体验及缓冲分别展示，保留绝对截止时间与整桌预计结束时间。
- 使用既有 `server_now`、本地接收时间和截止字段，不新增接口或持久化字段。共享数字格式化函数移至桌台时钟模块，管理员原导出保持兼容。
- 原 5 秒轮询保留，复用现有边界调度在截止后 100 毫秒校准；只允许一个在途请求。每秒只更新子组件，不反复刷新整个详情或处理结果提醒。
- 本地归零等待服务端阶段确认，失败保留零与重试入口；隐藏、卸载、关闭均清理定时器，过期的异步响应不能覆盖新状态。
- 需求规则已同步至 [桌台需求](../../01_requirements/table_session_module.md)；无后端业务、鉴权、API、数据库、版本或依赖变更。

## 验证

- 新增 11 项回归测试：客户端时间偏差、每秒无额外请求／提醒处理、多组体验及缓冲、付款截止失败、体验与结束阶段、缓冲归零、前后台恢复和卸载、初次失败重试、慢请求与过期响应隔离、付款截止等值边界、访客及管理员禁止读取。
- 完整前端：112 套件、864 项通过；TypeScript、全量 ESLint、新增样式 Stylelint 和差异空白检查通过。
- 现有微信编译监听生成的 `pages/table-history/index.js` 与 `.wxss` 已含倒计时组件及样式。
- 本轮未进行微信真机视觉／前后台实测；未运行后端测试，因未修改后端或接口契约。未执行真实支付、迁移或发布。

## 资源

测试与静态检查执行会话均已结束，无新增后台服务、临时文件、浏览器或其他需回收的资源；用户原有小程序编译监听保持运行。
