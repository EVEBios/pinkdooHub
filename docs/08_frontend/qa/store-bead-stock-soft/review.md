# 用豆库存「柔粉分区」视觉验证

final result: passed

范围：`/admin/pages/store-bead-stock/index` 的浏览、编辑、二次确认；不外推为真机、后端或发布验收。

## 设计依据与最终呈现

- 用户选定本轮显示顺序的 Option 3「柔粉分区」，补充要求保留“左侧色号及增减包数、右侧修改前 → 修改后”的独立变更行。
- 视觉目标：[selected-reference.png](selected-reference.png)，1453×1082。原始生成文件为 `exec-19bab8e1-960f-459b-b6d9-b6479f2c342d.png`；用户补充：[change-row-reference.png](change-row-reference.png)。这些图片仅作为设计证据，不进入小程序资源包。
- 实现源：`miniapp/src/admin/pages/store-bead-stock/index.tsx`、`index.scss`。没有新增业务状态、API、依赖、数据库结构或迁移。
- 采用白底、浅粉筛选分区与隔行台账；莓色选中态和按钮；没有绿色／黄色点缀、低库存红框或红点。

## 实屏与同输入对照

H5 production 构建通过临时 localhost 静态服务，以独立 Chrome 无头进程运行；当前没有可调用的 Codex 内置浏览器控制工具，因此采用本地 Chrome DevTools Protocol。所有 API 请求均由浏览器拦截为合成数据，没有连接真实数据库或使用真实登录凭据。整个 221 色列表仍由生产 React/Taro 组件与既有状态机渲染。

CSS viewport 为 390×844 和 320×844，deviceScaleFactor=1。生成三联图分别裁出手机内容，按 390 像素宽等比归一，去除外部标题和画布；高度约 847–855，保留原始比例，不纵向拉伸。实现截图均为390×844或320×844。

- 全页同图对照：[浏览](browse-comparison.png)、[编辑](edit-comparison.png)、[确认](review-comparison.png)。左为归一后的视觉目标，右为生产组件实屏。
- 重点对照：[编辑数字及加减](edit-focused-comparison.png)、[确认前后值及增减](review-focused-comparison.png)。生成图中文字尺度不是精确排版契约；以正常中文可读性、真实手机触控空间与项目字体为约束。
- 小屏：[浏览](browse-320.png)、[编辑](edit-320.png)、[确认](review-320.png)。完整四项状态和十项分组没有水平滚动；所有可用筛选和加减至少44px高。
- 边界：[390六位数](edit-long-number-390.png)、[320六位数](edit-long-number-320.png)、[320长数值确认](review-long-number-320.png)、[320列表末尾](edit-bottom-320.png)。最大值999999完整显示；列表最后A25/A26和说明可滚动到固定页脚上方。

## 比较历史与修复

1. 首轮实屏发现 [P2] H5内层输入数字靠上、加减高度受原生行高影响。证据 `edit-390-before.png`、`edit-320-before.png`。修复：内层 `.weui-input` 使用完整高度及居中，按钮显式行高1，最终高度44px。
2. 同输入对照发现确认清单前后数值及摘要视觉分量偏弱。修复：局部加强标题、摘要及前后值字号，不改全局令牌；最终见 `review-comparison.png`。
3. 修复后重新构建并拍摄两种宽度及长数字、滚动末尾；再次检查全图和局部图，上述问题均已解决，无剩余可执行的P0/P1/P2。

## 五项视觉检查

- 字体：沿用项目中文系统字体，莓墨正文、加粗色号、稳定数字；输入已居中，确认前后值与增减都清楚。小屏数字改为独立输入行，避免截断。
- 间距：筛选包在同一浅粉区；台账双列且两列交替底色按可见行计算，与库存值无关；确认逐条白色卡片及页脚按钮有独立间距。
- 颜色：只用暖白／浅粉／莓色，灰色用于辅助和禁用；0/1没有特殊色彩，不把增减方向混同低库存告警。
- 图像：目标无商品图片、插画或装饰素材，实屏使用真实组件；生成整图没有用作页面截图背景。保留原有更改文案及文本运算符，无新图标依赖。
- 内容：二次确认包含A1减少1包及3→2、A3增加3包及1→4、B1增加5包及0→5；增加8包、减少1包分别汇总。首次盘点仍标明未盘点，与0不同。

## 有意差异与限制

- 原图编辑加减的可见高度不足44px，实际界面保留44px触控，因此首屏可见编辑行数少于图片；在≤360px时数字独立成行。属于触控及最长数字的适配，不缩小目标来照搬图中密度。
- 合成列表完整包含221色；A组有26行，原图仅绘8个示例。因此真实浏览继续展示A9等，未盘点说明在列表末尾。实际合成数据未盘点28色，原图44色不是业务常量。
- 保留“开包即减1包”、取消调整、更改等已有说明；二次确认提示跟随内容流，未强制固定到页脚。这些保留规则不影响选定视觉层次。
- 新视觉的微信真机软键盘、安全区与原生组件最终表现仍由用户检查；H5截图和WXSS编译不代表真机视觉已通过。此前功能经用户确认有效。

## 行为与编译验证

- 模块Jest：3 suites / 12 tests通过，包含新增确认行同时显示增减及前后值、返回草稿不丢；保留既有首次盘点0、隐藏草稿、未知结果、版本冲突回归。
- TypeScript、局部ESLint、Stylelint与git diff --check通过。Jest仅保留既有Taro/React act工具链提示。
- H5生产构建通过，保留既有产物体积警告；微信既有watcher已输出更新文件，实际页面与分包依赖通过微信原生wcsc编译。未控制或重启用户开发者工具。
- 浏览器走过调整A1/A3、切B组调整B1、回A组、确认三条、返回继续、最大999999、恢复草稿、最终保存；二次确认前写请求0次，最终1次（仅合成API）。运行时异常0。详见 `checks.json`。
- 本次纯前端呈现改动未重复后端全量和其他小程序平台构建，不复用历史数字作本轮结果。

## Review checklist 与资源

本次只涉及页面结构、局部样式和相关回归；跨层、权限、事务、持久数据与接口不变，未引入新请求、数据源或依赖，既有用户未提交改动保留。无commit、push、发布或迁移。

任务临时Chrome、静态服务均已退出，临时浏览器配置及脚本/日志在收集结果后清理；原有微信开发者工具、Taro watcher、后端和Redis不归本次任务所有，保持运行。最终视觉图与本记录保留用于用户复核。

## 后续修正：页头按钮文字居中

用户提供微信截图指出“调整库存”文字偏上，说明前述H5全页检查没有覆盖微信原生行高差异。源码中该按钮仅自定义最小高度与字号，未复用双向居中的基础模式；现改为 `@include pd.button-base(...)`，同一按钮的“取消调整”一起修复。

新增编译后最终声明守卫，覆盖双向居中、行高及盒模型；本模块3组13项通过。独立Chrome以Taro宿主元素、默认2.555倍行高模拟原行为，测试44/70px高度、13/20px字号、两种文案、正常/禁用共16组。修复前模拟最大纵向偏移18px，修复后文字框中心偏移0px，数据见 `button-centering-checks.json`，图见 `button-centering.png`。本项只证明局部CSS布局；用户仍需检查微信实际显示，不能把浏览器模拟当作真机截图。TypeScript、局部ESLint/Stylelint和实际生成WXSS原生编译通过。

按钮规范已写入 DESIGN.md#buttons，前端架构和Review Checklist引用该唯一规范。临时验证浏览器与配置已退出并清理；无需数据库迁移。
