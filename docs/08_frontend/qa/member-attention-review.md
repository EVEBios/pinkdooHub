# 会员中心提醒入口 Option 2 · 2026-09-14

final result: passed

范围：用户选定的会员中心提醒入口视觉实现。此结果仅代表该组件的设计检查，不代表完整 H5 路由、微信真机或发布门槛验收。

## 视觉依据与页面证据

- 选定源图：`/Users/shenyijie/.codex/generated_images/01a09ef8-7bed-7530-bdf7-5a9d8c30483d/exec-ba789c7c-04f4-409e-8183-f8c683f5d1ec.png`，1076 × 1462 px。
- [归一化参考](member-attention/reference-390.png)：等比缩至 390 × 530 px。
- [实际页面](member-attention/member-390.png)：390 × 844 CSS px，截图 390 × 844 px，有效密度 1。
- [同屏对照](member-attention/comparison.png)：参考与实际各取 390 × 530，左参考、右实现。
- [入口局部对照](member-attention/comparison-entry.png)：两侧取 y=155、390 × 115 的相同区域，检查图标座、文字、角标、箭头和留白。
- [320 px / 两条提醒 / 99+](member-attention/member-320-multiple.png)。
- [320 px / 钱包失败](member-attention/member-320-wallet-error.png)：验证错误状态下提醒入口仍由深莓底承载。错误文案来自隔离夹具的模拟错误码，不作为真实钱包业务结果证据。

标准状态：林小豆／@xiaodou、余额 ¥500.00、桌台提醒 1、普通会员。用户与余额均为隔离 HTTP 夹具，未连接持久数据库、未操作真实资金或用户数据。

## 五项视觉检查

- 字体与层级：复用系统中文字体，标题 34 源 px、750 字重；说明 24 源 px、400 字重，独立成行、行高 1.5。布局容许说明换行。缩放后的字号与原会员页面保持一致。
- 间距与排版：390 下入口约 350.48 × 81.12 CSS px，左右各约 19.76 px；320 下入口 282 × 78 px。两条入口间隔 14 CSS px，角标与箭头不压缩，scrollWidth 分别等于 390 和 320，无横向溢出。原订单区分隔线和快捷入口保留。
- 色彩与对比：深莓纹理继续使用既有资产；入口采用浅粉 8% 透明填充与 40% 细描边，标题白色、说明 #f5dbe5，角标 #c83d4a。按压只加深底色，不改变几何尺寸。
- 图像与资产：桌椅使用授权 MDI `table-chair` 本地 144 px PNG，保留 Apache 2.0 许可证；与方案中的双椅图形存在库图标形状差异，是避免手绘近似且保持标准资产的明确适配。订单图标、白色箭头、纹理和钱包图案均复用原资产，无运行时图标依赖。
- 内容：桌台说明采用“查看待付款与桌台动态”；真实计数与无障碍整行标签保留，0 隐藏，超过 99 显示 99+，标签仍朗读实际数量。未新增虚构状态或操作。

首次有效同屏比较未发现需要迭代修正的 P0/P1/P2 组件视觉问题。保留了既有订单分隔线、品牌排版和钱包结构；源图在这些范围外的细微差异不扩大本次修改范围。

## 预览环境边界与交互

实际 Taro H5 构建通过。首次直接访问会员路由时，既有 H5 容器在身份初始化后停在 `translate(100%, 0)`；内置浏览器和 Chrome 均复现，控制台未报 JavaScript 错误。为完成本次组件视觉比较，隔离预览服务器仅在返回 HTML 时把 `.taro_router > .taro_page` 的 transform 固定为 none 并关闭 transition。该处理没有进入源码或小程序产物，也没有改动组件的排版与样式。完整 H5 初始路由仍需独立诊断，不能将本报告当作该问题已修复的证据。

浏览器通过实际入口进入 `/pages/commerce-attention/index?scope=tables`，显示“桌台提醒”及筛选内容；此过程控制台无错误。另验证 0 隐藏、两条提醒及 99+ 的可读性、钱包失败时的入口。权限、提醒读取时机和后端统计逻辑未修改，交互回归由 Jest 覆盖。

## AI Code Review Report

- Changes：`features/attention/index.tsx`、`style.scss` 增加会员专用外观；会员页传入 appearance；错误态深莓容器覆盖提醒与快捷区；新增桌椅图标、许可和两项会员回归测试。
- Architecture Check：通过。新增参数仅控制呈现，默认首页外观与原数据来源、过滤、跳转保持原状；没有后端或接口修改。
- Security Check：通过。无真实数据写入、凭据、支付、权限或迁移变更；图标从官方版本源获取并记录授权。
- Documentation Check：DESIGN.md、changelog、图标 README 与本报告同步。项目根 design-qa.md 附加本轮记录，保留历史记录。
- Test Coverage：会员与提醒 3 个套件、37 项测试通过；新增 2 项覆盖图标/整行跳转、两类路由、真实数量、99+、零提醒与钱包失败时的提醒。
- 静态检查：TypeScript、相关 ESLint、相关 Stylelint、git diff --check 通过。H5 构建通过；用户已有微信构建监听器已生成新样式、文案和图标。
- Action Items：无需版本升级、数据库迁移或新增依赖。未执行真机视觉/辅助技术验收；既有 H5 初始路由问题单列后续，不因本轮组件通过而视为已修复。

资源：隔离预览服务器与临时文件按任务归属回收；临时浏览器页关闭，视口覆盖恢复。用户既有微信构建监听器与本地后端保留。

## 追加修订：双椅图标

用户明确要求与选定预览一致的“一桌双椅”。已用独立 Image Gen 资产 `table-two-chairs-white.png` 替换原 MDI 单椅图标，并移除闲置原资产与许可证。图标保持 144 × 144 透明 PNG，页面仍使用原 64 源 px 图像槽位，布局、文案和跳转不变。

[资产检查图](member-attention/two-chairs-asset.png) 左侧为 32 px 实际显示尺寸、右侧为 96 px 放大检查；确认两把椅子、白色圆角笔画、透明边缘及居中。前文整页截图属于修订前历史证据，不代表最新图标。此次仅替换资产及引用，没有新增行为或测试；相关会员页回归重新运行，产物复制按文件摘要核对。

## 后续收口：P1 联合验收

已复现并修复上述 H5 身份初始化白屏：稳定购物车 Provider 的层级，只切换其启用状态。重新构建后真实浏览器刷新已登录会员中心，页面 transform 为 none、横坐标为 0；没有注入覆盖样式。详见 [联合验收记录](p1-joint-review.md)。
