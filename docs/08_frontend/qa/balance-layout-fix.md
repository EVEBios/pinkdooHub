# 会员余额卡排版修正 · 2026-09-13

final result: passed

## 截图审查与修复

1. **用户报告状态：[P2] 排版疏散、图片被拉伸。** 当前用户提供的截图为 582 × 194，已保存并打开 [原始证据](../../../design-qa-assets/balance-fix-user-before.png)。左侧状态与资金入口间有大段空白，钱包和金额偏右。代码确认未开通充值被做成完整高度按钮，正方形钱包 PNG 被 `60×64`／窄屏 `46×50` 非正方形容器加 `scaleToFill` 拉伸。仅凭原图不判断点击行为、实际 CSS 视口或全面可访问性。
2. **修复后零余额：[已通过]。** 未开通充值为静态状态文字；资金明细仍为完整 44px 触控入口，开通且账户可用时才出现独立充值按钮。钱包改为 `60×60`／窄屏 `46×46` 并使用 `aspectFit`，右侧图案与金额组合居中。[局部前后对照](../../../design-qa-assets/balance-fix-before-after.png) 左为用户原图，右为新浏览器截图，统一到 390px 宽、保留各自高度，明确展示卡片收紧效果，不把变矮当作同一高度的像素对照。新卡在 390px 视口约高 90.3px，钱包 DOM 外框 31.2×31.2px。
3. **金额变化与资金入口：[已通过]。** 在隔离 fixture 上用 UI 完成登录、切购物车与回会员刷新、资金明细与返回，确认零余额、128.00 和 1,000.00；[320px 上限状态](../../../design-qa-assets/balance-fix-320-max.png) 钱包外框 23×23px、金额单行，金额与右列的 scrollWidth 均未超出 clientWidth。最终内置浏览器 console error 为零。

## 视觉复核

- **目标与完整页面**：[修正后390px／零余额](../../../design-qa-assets/balance-fix-390.png)、[390px／128余额](../../../design-qa-assets/balance-fix-390-128.png)、[320px／零余额](../../../design-qa-assets/balance-fix-320.png)。浏览器截图像素尺寸分别等于 CSS 视口 390×844 和 320×844，有效截图密度 1。
- **目标对照**：[同屏整页比较](../../../design-qa-assets/balance-fix-target-comparison.png)，左侧为用户此前已选定最终稿的 390×844 归一化副本（原图853×1844），右侧为本次相同128余额状态；本轮范围仅为余额卡。上方身份／订单、资料和退出样式不扩大修改。
- **字体与文案**：保留字体栈、标题／动作／状态的层级和字号；状态行不再通过按钮高度定位。金额以字符串格式化并禁止换行，未引入金额计算。
- **间距与结构**：左侧三行集中，状态与资金入口之间为8源px间隔；右侧图案与金额横向成组并在列内居中；分隔线和金额垂直居中。消除旧按钮的额外留白，不靠重叠触控区或负间距压缩。
- **颜色**：保留已有深莓金额、柔玫瑰钱包、弱分隔线和瓷白背景。未修改素材颜色或使用新图案替换用户选定钱包。
- **图片质量**：复用同一透明PNG，等比渲染，纸钞与搭扣完整、没有裁切和拉伸；没有重新生成或截取界面作为资产。
- **交互／可访问性范围**：资金明细按钮保持44px高度；不可用充值不再伪装成可点击动作。三种充值能力／账户组合由自动测试覆盖。未以截图宣称全面可访问性合规。

## 验证与适用边界

- 会员页面与钱包 Hook 共2 suites／20 tests通过，包括未开通不显示按钮、已开通且可用时跳转、账户不可用时不显示充值入口。
- TypeScript、定向ESLint／Stylelint、H5构建通过；H5保留既有包体积建议，测试工具保留React act弃用提示。
- 微信 `--mode ci` 构建及产物检查通过：193文件、主包951194字节、分包559942字节、总计1511136字节，release_eligible=false。
- 未触碰真实账号／数据库／支付；没有后端API、依赖、迁移或版本变化。没有发布、commit或push；未做微信真机复验。
- 本次结论覆盖并修正上一轮余额卡的间距与图片比例结论；此前报告是历史证据，不再代表此处最终布局。

## 资源回收

临时服务PID54928已停止，复查该PID不存在且18766无监听；任务创建的内置浏览器标签已关闭、尺寸覆盖已恢复，构建／测试会话已结束。临时目录 `/tmp/pinkdoo-balance-fix` 已删除。未接管或停止用户已有服务，无遗留任务资源。
