# Gate A M10–M15 执行与验证计划

## 范围与前置条件

用户授权持续修复、提交、推送并按规划将 Gate A 迁移至 M15；需要密码时交接。当前持久状态以[已付款恢复记录](gatea_m9_paid_recovery_2026-09-15.md)及现场 records 为准。必须先完成 M9 的受保护恢复、验收、韧性、数据后独立恢复与 finalized lineage；不跨过失败记录直接迁移，不外推到共享或生产。

本分支以既有 M15 业务实现 `ec4c068` 为基础，合入 M9 恢复修复。没有因此认定已迁移或可以部署。旧 CI updater 与 source contract 只认识精确 M0–M9，新代码不能直接上传到现有发布器。

## 先完成发布工具能力

1. 冻结已 finalized M9 source 与 M15 target，区分历史 M9 验收和新候选验收；原证据格式继续可读。
2. 建立唯一权威 M0–M15 链与 M9→M15 执行契约，同时更新 source archive、镜像、迁移任务、operation、backup/restore、upgrade、stage、验收与 finalize 的支持范围。不能只改一个版本常量绕过拒绝。
3. 在一段停写窗口中逐个执行 M10–M15，逐步检查结构、Aerich 版本和保留数据。M15 代码只能在其完整依赖结构就绪后启动，不能在 M10/M11 中间结构上试运行最新版。
4. 原有字段按主键和原始列集合比较；M11 新增字段的合法默认值单独核验。新建表应为空，不回填旧提醒或虚构库存。现有全行 dump 摘要不能直接跨新增列比较。
5. 备份与独立恢复覆盖全部新增表、外键、索引及实际数据；Redis 仍以空实例验证。任一 DDL 部分完成时保留耐久记录，按实际结构前滚，不能假称事务回滚或重跑已完成步骤。
6. 先用本地合成存量数据做完整 M9→M15 演练，再运行当前候选自己的 CI。通过后才执行持久 Gate A 流程。

## 模块变更与定向测试

| 版本 | 迁移影响 | 先运行的测试 | 迁移后的关键检查 |
|---|---|---|---|
| M10 | 预约八个套餐字段可空 | `tests/reservation/test_optional_package.py` 及迁移契约 | 历史预约原值不变；带/不带套餐均可读写；客户端兼容空套餐 |
| M11 | Session/Occupancy 支持直接开台，新增来源字段及 FK | `tests/table_sessions/test_direct_table_migration.py`、`test_direct_table_session.py`、对应 MySQL 组 | 旧 Session source=order；既有桌码/付款/计时不变；直接开台、释放和 reconcile |
| M12 | 新增预约结果提醒表 | `tests/reservation/test_attention*.py`、对应 MySQL 组 | 不回填历史已处理结果；新状态改变与事件同事务；已读按用户隔离 |
| M13 | 提醒增加订单/会话关联与安全结果摘要 | `test_commerce_attention*.py`、`test_financial_attention.py` 及对应 MySQL 组 | 原预约事件及已读不变；付款/退款/释放提醒与业务同事务且幂等 |
| M14 | 新增不可变预约跟进记录 | `tests/reservation/test_followups.py`、`test_followup_migration.py`、对应 MySQL 组 | 不改变预约状态；并发版本、重放、权限和说明校验 |
| M15 | 新增门店库存余额/批次/逐色流水三表 | `tests/store_bead_stock/` 及其 MySQL 并发组 | 221 色未盘点为 packs=null/revision=0；不动商城10g库存；真实盘点/版本冲突/幂等 |

只修改发布编排时先运行受影响 release 测试；只修改模块时先运行本行测试与相关格式/类型检查。跨模块最终候选再运行一次完整后端、前端质量与生成契约检查。失败后仅重跑受影响范围；代码未变且证据有效时不重复全量测试或完整演练。

前端需要变更时按对应预约、桌台、提醒、跟进或门店库存测试目录验证。正式微信上传、灰度、发布以及真实微信资金不属于本次迁移；服务端上线不等同于小程序平台验收完成。

## 状态记录

本文件是执行计划，不是完成报告。M9 收口、M15 工具、隔离完整演练、CI、持久升级和数据后独立恢复分别留证；当前这些后续步骤尚未完成。

### 本地第一步：逐条迁移原语

单步任务现支持精确 M0–M15 文件清单，仍要求数据库恰好位于目标前一版，已在目标时只读 no-op。补齐双位数文件名后发现原清单比较混用了字典顺序与数字版本顺序，现只对文件集合比较使用同一排序，执行顺序继续来自固定清单。M10–M15 每个目标只传入该版本及更早文件，相关任务边界测试 `16 passed`。这一步尚未扩展 stage/backup/upgrade/finalize，源码前置门槛仍拒绝部署 M15；不应单独发布该工作分支。

M10 无套餐预约与 M11–M15 的现有迁移契约定向检查另有 `18 passed`（1.06 秒）；本轮没有重复全后端或前端套件。M9 已推进到 G 管理员 TTY 验收边界，具体状态见上述已付款恢复记录。

### 本地第二步：跨版本内容与完整结构

在一次性 MySQL 8.0.46 上用冻结 G/M9 的真实业务服务建立已付款/已关闭会话，从 M9 逐步执行 M10–M15。每步核对固定 M9 列投影摘要、精确版本链、完整列集合、索引与外键结构，以及新会话字段默认值/新增表为空。结构指纹与全部 16 个迁移文件摘要冻结在 `app/tasks/manifests/gatea_m9_m15_schema.json`；迁移文件变化会要求重新进行隔离核验。服务器只读 schema 核验与本地 M9 指纹完全相同，未在服务器执行 M15 代码或迁移。

`gatea_m15_snapshot` 在 MySQL 只读一致性事务内分批读取，每张表只输出行数和摘要；跨版本排除允许改变的新增字段，备份/恢复则比较包含 Aerich 内容在内的全部列。候选宿主可以只用标准库验证快照，不依赖安装 ORM 或 MySQL 驱动。

新增快照契约测试 26 项通过，覆盖旧内容/数量/版本/结构/默认值变化、额外业务数据、非法标识符和过期迁移 manifest。完整实际链验证在启用结构指纹拒绝后再次通过，临时 MySQL/tmpfs 均清理并复核。

备份模块现接入 M15 专用全内容快照，继续保留 M7/M8/M9 原格式。关联检查最初 86 项通过，1 项失败是测试期待内层错误而入口正确返回脱敏外层错误；修正断言后只重跑受影响的创建/恢复 5 项，全部通过。M15 恢复内容不同仍清理临时实例且不发布成功记录。此能力仍依赖尚待完成的 M15 deployment/replay、stage、CI 完整演练和验收/finalize 入口，不可单独部署。

### 本地第三步：M15 stage 与独立制品

新增 schema 4 的 M9→M15 stage profile，固定 source 为已 finalized G，并要求当前候选自身的 `gatea-m9-m15-updater` 制品。制品必须包含逐步快照、目标备份/恢复、运行核验、完整清理结果和互相绑定的文件摘要；旧 M7→M9 制品不能替代。GitHub source tree 核验后，再用安装源码中的标准库快照契约检查迁移 manifest 和保留内容。历史三种 stage profile 保持原含义。

原 candidate 测试 258 项通过；新制品拒绝路径与 M9/M15 实际 stage 写入共 11 项通过。额外验证两种安装记录重新从磁盘读取，确认新格式可被后续入口使用。升级编排和 CI 生成器尚待接入，仍不可将当前开发分支直接用于持久升级。

### 本地第四步：停写升级与 M15 启动门槛

新增 `scripts.release.gatea_m15_upgrade`：默认只读计划；apply 绑定 G/M9 finalized、当前/live 指针、新鲜同 ID 备份/独立恢复、M15 stage 与镜像。停写后再次比较备份内容，逐条执行 M10–M15 并耐久保存每一步快照；全部保留校验通过后仅切换镜像配置，发布升级与只读重放记录，业务服务继续保持停止，由受控 app-up 启动。失败时不降级、不重跑，不删除 pending。

operations/candidate 均识别 `m15-upgrade.pending`，任何遗留记录阻止其他发布操作和启动。M15 app-up 要求真实升级/重放及完整结构契约，同时允许升级后增加合法业务数据。升级记录绑定 source finalized、stage、配置切换、源备份和恢复记录摘要，历史 M9 读取路径保持原义。宿主入口使用纯标准库，已在关闭 site-packages 的 Python 中验证导入。

release 定向回归 489 项通过。新增整段编排用例首次因 macOS 测试环境不允许 root 文件归属操作而失败；仅模拟该系统调用后六条成功/失败/只读路径通过。后续升级与快照契约 56 项通过，源 finalized/current/live/依赖校验新增 6 项通过。实际容器级 M15 备份恢复、CI 制品生成和 M15 验收/finalize 仍未完成，当前分支不可持久部署。


### 本地第五步：实际容器演练与定向修正

在没有宿主 Docker socket、没有对外端口的独立 Docker daemon 中，以 `db495c8` 完整运行真实 G/M9 → M10–M15、五服务启动、SQL/图片备份和独立恢复。全部迁移快照与恢复内容匹配；首次运行总体为失败，原因是清理误加载未使用的 bootstrap 配置。第二次清理确认无残留，原始失败证据保留，未改写为全流程通过。修复只让 M15 清理排除 bootstrap；使用真实临时容器、网络和数据卷单独复现并验证修正，未重复完整迁移。

额外运行预约、桌台、门店库存的真实 MySQL 定向测试：66 项通过。测试库初始化等待改为 TCP 查询成功，避免将初始化期间的临时进程误判为就绪。上述实例及其数据卷均已删除并核验；独立 daemon 暂留用于本轮最终检查，结束时统一回收。

### 本地第六步：M15 验收、韧性和最终收口

新增 M15 独立验收入口，读取预约、提醒、跟进和门店库存契约；221 色保持未盘点，不写入虚构库存。管理员直接开台、同意图重放、释放及响应丢失回收已通过实际 ASGI 定向测试。验收需管理员在隐藏 TTY 输入密码，日志仅保存安全计数，管理员会话最后撤销。

新增 M15 韧性与 finalize 入口，绑定同一候选的升级、管理员验收、MySQL/Redis 故障恢复、应用重启，以及验收后同 ID 备份/独立恢复。停写再次比较全部内容和图片后恢复五服务，最后才推进 current 和 finalized lineage。失败保留 pending，已发布 current 或最终记录但尚未删除 pending 的中断可按同一证据重入。18 条收口/韧性成功、拒绝与恢复路径通过。

以上是本地实现与验证状态。M15 仍须当前候选自己的 9 项 CI、制品校验、持久环境升级、管理员验收和数据后独立恢复，不以本地结果代替持久环境完成证据。


### CI 接入与 Review

源码预检按精确 M0–M9 / M0–M15 选择已实现的发布 profile，并在安装依赖前验证迁移清单、目标版本、G/M9 祖先关系、冻结文件摘要及验收/恢复/收口入口。只读取 Git/AST/JSON，不执行被检代码。后端源码契约测试和 updater 均获取完整 Git 历史；原九个 required job 名称不变，updater 内容切为 M9→M15，只有完成迁移、独立恢复、清理和敏感扫描后才生成可上传 summary。旧 M9 ref 仍可通过其原契约。

Review 范围为本轮发布工具、快照与 CI 连接：业务 API/层次和既有 M10–M15 表设计未另行改写；保留历史证据格式和保护 pending，秘密只经官方入口读取且不入报告；没有新增生产依赖或版本标签。迁移仍是正式 Gate A 部署步骤，CI/本地证据各自标记来源。源码预检 11 项、CI 工作流 8 项、收口/韧性 18 项定向检查通过；最终集合回归另行记录，不能将历史数字视为当前候选的 CI 通过数。


### 提交前集合回归与资源回收

后端集合首次为 `3450 passed, 39 failed`（259.51 秒）：39 条失败全部属于历史 M9 升级夹具误引用当前 M15 清单。夹具改为历史 M9 发布器的固定清单后，仅重跑该组，`129 passed`；未重复其余通过模块。实际 MySQL 三组业务测试为 `66 passed`。

前端跟进/资金提醒测试仅清空 Promise 队列，未等待 Taro Web Component 的动画帧提交，曾在并行负载时出现 DOM 尚未就绪。两处测试补等待实际渲染帧，生产 UI 不变；最终原全局测试配置下 `124 suites / 943 tests passed`（19.316 秒）。类型、代码/样式检查、CI policy 和 OpenAPI 导出一致性通过。本地前端使用可用的 Node 24.19.0，远端仍必须使用项目锁定的 24.13.0/11.6.2，不能将本地结果替代其检查。

本地独立 daemon 内的容器、数据卷及任务网络为空，随后停止并删除外层容器 `pinkdoo-m15-private-20260915`、其专属数据卷及本轮 runner/dind 镜像标签，复核均已移除。测试依赖链接和传输 bundle 已删除；保留验收证据文件，用户原开发 Redis 和共享镜像缓存未改动。
